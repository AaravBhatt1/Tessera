# Tessera

Tessera aims to be an app that declutters yout MacOS workspace with the press of a keyboard shortcut (or button on the toolbar). 
It lays out all open windows in non-overlapping positions with an optimal size.
Users can also configure the app using a programming language to define preferences on how they want their windows to be placed.
